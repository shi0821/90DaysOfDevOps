import React from "react";
import { motion } from "framer-motion";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Mail, Github, User } from "lucide-react";

export default function Portfolio() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-indigo-100 via-white to-purple-100 p-6">
      <div className="max-w-5xl mx-auto">
        {/* Hero Section */}
        <motion.div
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="text-center py-16"
        >
          <h1 className="text-5xl font-bold text-gray-800 mb-4">
            Hi, I'm Shivani Kesharwani 👋
          </h1>
          <p className="text-lg text-gray-600 max-w-2xl mx-auto">
            A passionate learner exploring DevOps, Linux, Git, and Cloud technologies.
            I love building, automating, and solving real-world technical problems.
          </p>
          <div className="mt-6 flex justify-center gap-4">
            <Button className="rounded-2xl px-6 py-2 text-base">
              <Mail className="mr-2 h-4 w-4" /> Contact Me
            </Button>
            <Button variant="outline" className="rounded-2xl px-6 py-2 text-base">
              <Github className="mr-2 h-4 w-4" /> GitHub
            </Button>
          </div>
        </motion.div>

        {/* About Section */}
        <Card className="rounded-2xl shadow-lg mb-10">
          <CardContent className="p-8">
            <div className="flex items-center gap-3 mb-4">
              <User className="h-6 w-6 text-indigo-600" />
              <h2 className="text-2xl font-semibold">About Me</h2>
            </div>
            <p className="text-gray-600 leading-relaxed">
              I am currently learning Linux commands, Git workflows, GitHub Actions,
              and cloud platforms like AWS. My goal is to become a skilled DevOps
              Engineer and work on scalable, automated infrastructure systems.
            </p>
          </CardContent>
        </Card>

        {/* Skills Section */}
        <Card className="rounded-2xl shadow-lg mb-10">
          <CardContent className="p-8">
            <h2 className="text-2xl font-semibold mb-6">Skills</h2>
            <div className="grid grid-cols-2 md:grid-cols-3 gap-4 text-center">
              {[
                "Linux",
                "Git & GitHub",
                "GitHub Actions",
                "AWS Basics",
                "Shell Scripting",
                "CI/CD Concepts",
              ].map((skill, index) => (
                <motion.div
                  key={index}
                  whileHover={{ scale: 1.05 }}
                  className="bg-indigo-50 rounded-2xl p-4 shadow"
                >
                  {skill}
                </motion.div>
              ))}
            </div>
          </CardContent>
        </Card>

        {/* Footer */}
        <footer className="text-center text-gray-500 py-6">
          © {new Date().getFullYear()} Shivani Kesharwani | Built with ❤️
        </footer>
      </div>
    </div>
  );
}
